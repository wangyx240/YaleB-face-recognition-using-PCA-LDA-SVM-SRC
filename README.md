# Simple Face Recognition using PCA, LDA, SVM & SRC based on YaleB dataset

This project include 6 parts:
    PCA + NN
    LDA + NN
    SVM
    SRC
    PCA/LDA/HOG/LBP + SVM
    PCA/LDA/HOG/LBP + SRC
